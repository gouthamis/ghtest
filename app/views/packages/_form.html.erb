<%= form_for(@package) do |f| %>
  <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
   <%= f.fields_for :fields do | j |%>
    <fieldset>
    <%= j.label :field_type, %w[text_field check_box] %><br>
    <%= j.text_field :name, placeholder: "field_name" %>
    <%= l.hidden_field :_destroy%>
    <%= link_to "[remove]", '#', class: "remove_fields"%>
   </fieldset>
   <%end%>
   
   <%= link_to_add_fields "Add_field", f, :fields%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
